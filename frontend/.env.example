# Frontend Environment Variables
#
# With Vite, the frontend reads all runtime configuration from
# window.MediaCMS injected by Django templates — no env vars are
# needed for normal development or production builds.
#
# Start the dev server:  npm run dev
# Production build:      npm run build
